# DeepSeek 验证记录

目标平台：Windows x64。

## Harness 更新功能：2026-09-18

- 66 项 Node 自动测试通过，覆盖既有 Chat 功能和新增的发布选择、预览版、精确版本、来源与完整性校验、运行中保护、更新失败、取消、退出、恢复、防降级、认证及独立运行时。
- 真实 Electron 更新按钮从官方 GitHub 检测到 `dsh-v0.1.6-alpha.2`，从官方 npm 下载对应精确版本，完成依赖安装、限定原生安装脚本、本地组件检查、临时目录内的启动与认证验证，最后切换当前引擎。
- 更新后的引擎在开发应用和最终打包 EXE 中均成功启动并载入官方 Harness 页面。认证参数交换后从 URL 移除；引擎运行时，界面和后端均拒绝更新，不会终止现有进程。
- 点击停止后，视图和本地端口关闭；恢复上次引擎、重启保留恢复结果、再切回已缓存新引擎均通过。
- 下载期间点击取消：界面与后端均显示取消，临时引擎清理，当前引擎未改变。另一次下载中退出应用：测试所属进程全部结束，没有激活未完成的引擎。
- 全程使用独立测试配置与临时工作区。Chat 设置与历史逐字节保持不变；重启后模型选择保留。
- 最终打包目录检查通过：随包 npm 可执行，依赖与许可证完整；独立 Node 下 Koffi、Sharp、Harness 本地子进程、node-pty / ConPTY 及进程清理正常。
- 最终独立 EXE 的 Chat 输入栏模型选择回归通过，包括真实本机 HTTP / SSE 请求、保存设置时的竞态、重启持久化和窄窗口布局。没有向模型服务发送测试请求。

## 既有功能验证：2026-09-15

以下记录对应之前发布的桌面应用，不代替本轮更新功能的验收：

- 26 项 Node 自动测试：流式分片、中文解码、错误与取消、模型与深度思考参数、密钥加密、设置与历史持久化、Harness 生命周期，以及重命名后的用户数据兼容。
- Electron 窗口端到端检查：Chat / Harness 切换、设置、搜索、Markdown 显示、深色主题、关闭保存。
- 本机模拟模型服务的真实 HTTP / SSE 往返；没有向 DeepSeek 发送测试请求。
- 输入栏模型选择：立即切换后发送的 HTTP 请求使用所选模型；保存连接设置、重新加载和重启后保留选择；延迟设置写入不会覆盖之后切换的模型或请求参数；窄窗口深色布局及键盘焦点正常。
- 当次 `release/win-unpacked/DeepSeek.exe` 独立启动、默认模型设置和资源加载。
- 当次打包目录里的 Node 与 Harness 原生依赖：Koffi、Sharp、本地子进程、ConPTY 终端及清理。构建的 `afterPack` 检查会自动执行这些验证。
- 当次打包 EXE 启动随包 Harness，认证后载入官方页面，切换模式与设置弹窗时正确隐藏 / 恢复页面；停止后页面销毁、端口关闭，测试应用正常退出。

## 验证限制

- 未使用真实 DeepSeek API Key 验证模型回复、计费或长时间任务。
- 未在另一台干净 Windows 机器通过安装向导安装和使用。
- 未验证 macOS / Linux 分发；交付目标为 Windows x64。
- 独立的更新检查使用临时配置，不会用真实用户数据验证所有历史格式、第三方插件和任务的上游迁移兼容性。
- 引擎恢复只切换程序，不恢复配置或会话数据，也不保证旧版能读取新版迁移后的数据。

## 交付边界

这是个人开发的桌面外壳。Chat 使用独立对话界面，Harness 编程模式由官方 Harness Web UI 提供。两者的模型配置分开保存。

应用内更新功能针对官方已发布的 Harness 引擎，包括预发布；用户点击后执行下载、验证和切换。Chat、Electron、内置 Node.js 和整个桌面应用仍通过新的安装包更新。未实现账号云同步、语音、图片生成或整个应用的自动更新；当前安装包没有代码签名。
